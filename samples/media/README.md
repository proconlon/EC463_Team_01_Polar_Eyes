# Sample Media

Drop visual demo assets here.

Suggested file names:

- `demo_trigger.gif`
- `sample_photo.jpg`
- `sample_event.mp4`

If the raw media is too large for git, keep this folder for thumbnails and add an external link in `samples/README.md`.
