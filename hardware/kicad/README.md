# KiCad Sources

Place the complete KiCad project here.

Expected files:

- `*.kicad_pro`
- `*.kicad_sch`
- `*.kicad_pcb`
- custom symbol/footprint libraries (if used)

Keep this folder as source-of-truth for hardware revisions.
